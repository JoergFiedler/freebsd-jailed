{{ ansible_managed | comment }}

poudriere: {
    url: "{{ jail_build_server_url }}",
    mirror_type: "pkg+https",
    signature_type: "pubkey",
    pubkey: "/usr/local/etc/ssl/poudriere.pub",
    enabled: {{ jail_build_server_enabled }},
    priority: 100
}
