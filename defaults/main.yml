---
# defaults file for jailed

jail_name: '{{ jail_net_ip }}'
jail_domain: 'darkcity'
jail_net_if: 'lo0'

syslog_server: ''

host_build_server_enabled: 'yes'
host_build_server_url: 'https://s3-eu-west-1.amazonaws.com/vastland.moumantai.de/public/FreeBSD/packages/freebsd-10_2_x64-HEAD'
host_build_server_pubkey: 'poudriere.pub'

