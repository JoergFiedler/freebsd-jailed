---
# defaults file for jailed

jail_name: '{{ jail_net_ip }}'
jail_domain: 'darkcity'
jail_net_if: 'lo0'

syslog_server: ''

ssmtp_forward_address: 'freebsd-ansible-demo@maildrop.cc'
ssmtp_forward_mailhub: 'mail.maildrop.cc'
